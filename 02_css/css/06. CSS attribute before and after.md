The `::before` and `::after` “attributes” you’re asking about are actually CSS pseudo‑elements, not normal HTML attributes. They let you insert extra content (like icons, text, or shapes) before or after an element’s content using only CSS.
