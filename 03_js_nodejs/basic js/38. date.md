```js
const d = new Date(2025, 1, 30, 10, 30, 0); // Jan 30, 2025, 10:30:00


console.log(mydate.toDateString());
console.log(mydate.toTimeString());

console.log(mydate.toLocaleTimeString());
console.log(mydate.toLocaleDateString());


d.getFullYear();        // 2025
d.getMonth();           // 0 (January)
d.getDate();            // 30
d.getDay();             // 4 (Thursday, 0 = Sunday)
d.getHours();           // 10 (because 10,30,0 → 10 in 24h format)
d.getMinutes();         // 30
d.getSeconds();         // 0
d.getMilliseconds();    // 0
```


to get the current time,data,year
```js

const d = new Date()
console.log(d.getFullYear());
console.log(d.getMonth());
console.log(d.getDate() );
console.log(d.getDay());
console.log(d.getHours());
console.log(d.getMinutes());
console.log(d.getSeconds());
console.log(d.getMilliseconds());


const nowMs = Date.now();          // current time in ms
```

