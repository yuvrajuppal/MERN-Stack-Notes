```js
export const getwallpaperPerCategory = async (req, res) => {
try {
let { categoryid } = req.body;
if (!categoryid) {
res.status(400).json({
success: false,
message: 'categoryid not Entered'
})
}
const qry = `SELECT id,wallpaper_link FROM wallpaper_info WHERE category = ?`
const [row] = await pool.execute(qry,[categoryid])
res.status(200).json({
success: true,
row
})
} catch (err) {
senderror(res, err)
}
}
```

