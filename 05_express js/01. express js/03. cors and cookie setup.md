```
npm i cors
```


```
npm i cookie-parser
```


allow all origin in app.js
```js

app.use(cookieParser());

app.use(
	cors(
		{
		origin: (origin, callback) => {
		if (!origin) {
		return callback(null, true)
		}
		return callback(null, origin)
		},
		credentials: true,
		}
	)
)
```
