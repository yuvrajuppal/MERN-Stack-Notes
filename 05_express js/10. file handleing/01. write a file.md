```js
import fs from 'fs'
fs.writeFileSync("./hello.txt","lol world")
```

	or
for better code
```js
import fs from 'fs/promises'
let whirefile = async () => {
try {
	await fs.writeFile("./hello.txt", "hello world")
} catch (err) {
	console.log(err.message);
}
}
whirefile();
```

writing json file
```js
import fs from 'fs/promises'
import { json } from 'stream/consumers';
let whirefile = async () => {
	try {
		let userdata = {
		username: "yuvraj",
		age: 20,
		skill: ["python", "java", "js"]
		}
		await fs.writeFile("./hello.json", JSON.stringify(userdata, null, 2), 'utf8')
		console.log('done he');
	} catch (err) {
		console.log(err.message);
	}
}
whirefile();
```