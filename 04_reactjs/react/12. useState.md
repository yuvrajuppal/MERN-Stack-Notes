`useState` is a React Hook that lets you add state (like a variable that changes over time) to a functional component so that the component can re‑render when that state changes.
```jsx
function App() {
	const [username,set_username] = useState("")
	return (
		<div>
		<div>Name : {username}</div>
		<button onClick={()=>{
		set_username("yuvraj")
		}}>setname</button>
		</div>
	)
}
```


