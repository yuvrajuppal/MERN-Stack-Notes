```
props are basic 
like a parameters of the function of a
componenets
```


```jsx
import React from 'react'
import style from './card.module.css'
function Card({dogname,imagelink}) {
return (
<div className={style.containder}>
<div className={style.imagediv}>
<img src={imagelink} className={style.imagedisplay}/>
</div>
<div className={style.dogname}>{dogname}</div>
</div>
)
}

export default Card

```

```jsx
<Card dogname={""} imagelink={""}/>
```
