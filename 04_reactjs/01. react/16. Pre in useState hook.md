```jsx
import React, { useState } from 'react'
import Navbar from './components/Navbar'
import Footer from './components/Footer'
import Card from './components/Card'
import style from './app.module.css'
import Buttonclick from './components/Buttonclick'
function App() {

const [counter, set_counter] = useState(0)

const counterplus = () => {
	set_counter((pre)=>{
	return pre +1
	})
	set_counter((pre)=>{
	return pre +1
	})
	set_counter((pre)=>{
	return pre +1
	})
}
const countersub = () => {
	set_counter(counter - 1)
}
return (
	<div>
	<div>{counter}</div>
	<ClickButton functionva={counterplus} context={"increament"}/>
	<ClickButton functionva={countersub} context={"decreament"} />
	</div>
)
}
export default App

const ClickButton = ({ functionva,context }) => {
return (
<button onClick={functionva}>{context}</button>
)
}
```